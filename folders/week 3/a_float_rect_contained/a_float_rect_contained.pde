void setup () {
  size (400, 400);
}

void draw () {
  background (0);
  float x = (sin(frameCount * 0.01) * 300;
  float w = (200);

  rect (x, x, w, 100);
  rect (x, 300, w, 100);
  rect (x*0.5, x, w, 100);
  rect (x, x, w, 100);

}
// whereever you can, use variables to simplify equations

